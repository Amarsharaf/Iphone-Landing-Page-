let bigPhoto = document.querySelector(".big")
let page = document.querySelector(".container")

function changeBigPhoto(src) {
    bigPhoto.src = src
}
function changeColor(color) {
    page.style.background = color
}